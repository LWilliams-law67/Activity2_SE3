name: "activity2"
author: "Luke Williams"

inputs:
  AGE:
    required: true
    default: 0
  NAME:
    required: true
    default: "you"
  YEARS:
    required: true
    default: 5

# Controls when the action will run. 
runs:
  using: "docker"
  image: "Dockerfile"
